version: '3'

services:
  kehanet:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1314:1314"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
